views:
  - type: table
    name: Table
    filters:
      and:
        - category.contains(link("Texts"))
        - '!file.name.contains("template")'
    order:
      - file.name
      - Type
    sort:
      - property: Type
        direction: ASC
